{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "rum/_websocket-schema.json",
  "title": "RumWebSocket",
  "type": "object",
  "description": "WebSocket-specific connection details. Only present when resource.type is 'websocket'.",
  "properties": {
    "connection_id": {
      "type": "string",
      "description": "UUID generated by the SDK at WebSocket construction time.",
      "readOnly": true
    },
    "start_time": {
      "type": "integer",
      "description": "Unix timestamp (ms) when the WebSocket connection opened.",
      "minimum": 0,
      "readOnly": true
    },
    "end_time": {
      "type": "integer",
      "description": "Unix timestamp (ms) when the connection closed or was flushed.",
      "minimum": 0,
      "readOnly": true
    },
    "start_view_id": {
      "type": "string",
      "description": "View ID where the WebSocket connection opened.",
      "readOnly": true
    },
    "end_view_id": {
      "type": "string",
      "description": "View ID where the connection ended; may equal start_view_id.",
      "readOnly": true
    },
    "closure_reason": {
      "type": "string",
      "description": "How the SDK learned the connection ended.",
      "enum": ["close_event", "session_end"],
      "readOnly": true
    },
    "close_code": {
      "type": "integer",
      "description": "WebSocket close code, per RFC 6455. Only set when closure_reason is 'close_event'.",
      "readOnly": true
    },
    "close_reason": {
      "type": "string",
      "description": "WebSocket close reason string. Only set when closure_reason is 'close_event'.",
      "readOnly": true
    },
    "was_clean": {
      "type": "boolean",
      "description": "Value of CloseEvent.wasClean — false on abnormal closes (e.g. close code 1006). Only set when closure_reason is 'close_event'.",
      "readOnly": true
    },
    "messages_in": {
      "type": "object",
      "description": "Aggregated stats for incoming messages over the lifetime of the connection.",
      "properties": {
        "count": {
          "type": "integer",
          "description": "Number of incoming messages received.",
          "minimum": 0,
          "readOnly": true
        },
        "size": {
          "type": "integer",
          "description": "Total bytes received.",
          "minimum": 0,
          "readOnly": true
        }
      },
      "readOnly": true
    },
    "messages_out": {
      "type": "object",
      "description": "Aggregated stats for outgoing messages over the lifetime of the connection.",
      "properties": {
        "count": {
          "type": "integer",
          "description": "Number of outgoing messages sent.",
          "minimum": 0,
          "readOnly": true
        },
        "size": {
          "type": "integer",
          "description": "Total bytes sent.",
          "minimum": 0,
          "readOnly": true
        }
      },
      "readOnly": true
    },
    "time_to_first_message_in": {
      "type": "integer",
      "description": "Milliseconds from open to first incoming message.",
      "minimum": 0,
      "readOnly": true
    },
    "time_to_first_message_out": {
      "type": "integer",
      "description": "Milliseconds from open to first outgoing message.",
      "minimum": 0,
      "readOnly": true
    },
    "last_message_at": {
      "type": "integer",
      "description": "Unix timestamp (ms) of the last message (in or out).",
      "minimum": 0,
      "readOnly": true
    },
    "max_gap": {
      "type": "integer",
      "description": "Longest silence in milliseconds between consecutive messages.",
      "minimum": 0,
      "readOnly": true
    },
    "idle_duration_before_close": {
      "type": "integer",
      "description": "Milliseconds of silence before the connection closed.",
      "minimum": 0,
      "readOnly": true
    },
    "buffered_amount_max": {
      "type": "integer",
      "description": "Peak WebSocket.bufferedAmount in bytes.",
      "minimum": 0,
      "readOnly": true
    },
    "protocol": {
      "type": "string",
      "description": "Negotiated WebSocket sub-protocol from WebSocket.protocol once the connection is open. Empty string when no sub-protocol was selected.",
      "readOnly": true
    }
  },
  "readOnly": true
}