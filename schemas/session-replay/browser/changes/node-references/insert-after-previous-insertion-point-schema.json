{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "session-replay/browser/changes/node-references/insert-after-previous-insertion-point-schema.json",
  "title": "InsertAfterPreviousInsertionPoint",
  "description": "A zero insertion point. Inserting a node at zero indicates that the new node should be inserted after the node with an id one lower than the new node, as if the DOM method after() is being used. Using a zero insertion point repeatedly is thus a quick way to insert a sequence of sibling elements.",
  "const": 0
}
