{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "session-replay/browser/changes/node-references/next-sibling-insertion-point-schema.json",
  "title": "NextSiblingInsertionPoint",
  "description": "A negative integer insertion point. Inserting a node at negative integer -N indicates that the node should be inserted as the next sibling of the node with an id N lower than the new node.",
  "type": "integer",
  "maximum": -1
}
