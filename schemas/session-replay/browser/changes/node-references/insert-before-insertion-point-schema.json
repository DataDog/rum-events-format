{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "session-replay/browser/changes/node-references/insert-before-insertion-point-schema.json",
  "title": "InsertBeforeInsertionPoint",
  "description": "A negative integer insertion point. Inserting a node at negative integer -N indicates that the new node's next sibling is the node with an id N lower than the new node, and that we should insert the new node before its next sibling, as if the DOM method insertBefore() was being used.",
  "type": "integer",
  "maximum": -1
}
